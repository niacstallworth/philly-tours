# StreamingAssets

Place the generated Quest spatial runtime payload here for Unity ingestion.

Expected filename:

- `quest-hero-scenes.json`
- `quest-scenes.json`

Build the payload from the repo root with:

```bash
CITY=philly npm run spatial:quest:build
```
